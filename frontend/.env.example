# 분리배포(decoupled)에서 dev 프록시가 가리킬 Spring 백엔드 오리진.
# jar 통합배포(BUILD_TARGET=static)에서는 같은 오리진이라 불필요.
BACKEND_ORIGIN=http://localhost:8080

# 프론트가 API를 부를 베이스. 비우면 상대경로 /api (dev 프록시 또는 같은 오리진).
# 분리배포에서 다른 도메인 백엔드를 직접 부를 때만 채운다(이 경우 Spring CORS 오리진 고정 필요).
NEXT_PUBLIC_API_BASE=
